{
    "name": "blog",
    "private": true,
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "start": "lerna run start --parallel",
        "server:dev": "yarn workspace @blog/server run dev"
    },
    "devDependencies": {
        "lerna": "^3.22.1",
        "prettier": "^2.2.1"
    }
}
